apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: kustomization

generatorOptions:
  disableNameSuffixHash: true

resources:
  - pv-minio.yaml
  - pv-postgres.yaml
  - minio-secret.yaml
  - postgres-secret.yaml
  #- postgres-secret.yaml
  - s3-secret.yaml
